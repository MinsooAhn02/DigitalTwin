# Python 3.13 이상 필요
# torch/tensorrt는 Makefile(make dev)이 자동 설치 — 여기서 제외
#
# 설치: pip install -r requirements.txt

# ── Deep Learning / Vision ──────────────────────────────────────────
ultralytics>=8.4.0          # YOLOv8 detection (8.4.41 검증)
supervision>=0.27.0         # ByteTrack + annotators (0.27.0.post2 검증)

# ── Web Framework ────────────────────────────────────────────────────
fastapi>=0.111.0            # (0.136.1 검증)
uvicorn[standard]>=0.29.0   # (0.46.0 검증)
websockets>=12.0            # (16.0 검증)

# ── Computer Vision ───────────────────────────────────────────────────
opencv-python-headless>=4.9.0   # (4.13.0.92 검증)

# ── HTTP / Async ──────────────────────────────────────────────────────
httpx>=0.27.0               # ITS OpenAPI 호출 (0.28.1 검증)
aiofiles>=23.2.1            # (25.1.0 검증)

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # (1.2.2 검증)
pydantic>=2.7.0             # (2.12.5 검증)
